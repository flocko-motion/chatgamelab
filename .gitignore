server/__debug_bin*

.grepai
.todos

# Environment files (use .env.example as template)
.env
bin
*/__debug_bin*
server/cgl
server/cgl
.planning/
.grepai/
.todos/
.sidecar/
.sidecar-agent
.sidecar-task
.sidecar-pr
.sidecar-start.sh
.sidecar-base
.td-root

# sindri hub artifacts (agent worktrees + hub state) — not for the repo
.worktrees/

# Claude Code per-user local settings — not for the repo
.claude/settings.local.json
